<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PhoneAudioSender" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">@color/sender_accent</item>
        <item name="android:navigationBarColor">@color/sender_background</item>
        <item name="android:statusBarColor">@color/sender_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.PhoneAudioSender" parent="Base.Theme.PhoneAudioSender" />
</resources>
